# Быстрая настройка расширений FinanceFlow

## ✅ Что уже настроено автоматически:

1. ✅ **Widget Extension** - полностью функциональный виджет с балансом
2. ✅ **App Intents Extension** - Siri Shortcuts для добавления транзакций
3. ✅ **App Group** - синхронизация данных между приложением и виджетом
4. ✅ **Обработка транзакций из Siri** - автоматическая обработка команд

## 🔧 Что нужно сделать в Xcode:

### 1. Добавить файлы в таргеты

#### Для Widget Extension (`FinanceFlowWidget`):
- ✅ `FinanceFlowWidget.swift` - уже добавлен
- ✅ `SharedData.swift` - нужно добавить в таргет
- ✅ `FinanceFlowModel.xcdatamodeld` - нужно добавить в таргет

#### Для App Intents Extension (`FinanceFlowIntents`):
- ✅ `FinanceFlowIntents.swift` - уже добавлен

### 2. Настроить App Group

1. В Xcode выберите каждый таргет:
   - `FinanceFlow` (основное приложение)
   - `FinanceFlowWidget`
   - `FinanceFlowIntents`

2. Для каждого таргета:
   - Signing & Capabilities → + Capability
   - Выберите **App Groups**
   - Добавьте: `group.com.financeflow.app`

**ВАЖНО:** Сначала создайте App Group в [Apple Developer Console](https://developer.apple.com):
- Certificates, Identifiers & Profiles → Identifiers → App Groups
- Создайте новый с ID: `group.com.financeflow.app`

### 3. Добавить зависимости

#### Для Widget Extension:
В Build Phases → Link Binary With Libraries добавьте:
- WidgetKit
- CoreData

#### Для App Intents Extension:
В Build Phases → Link Binary With Libraries добавьте:
- AppIntents

### 4. Настроить Core Data для виджета

Убедитесь, что `FinanceFlowModel.xcdatamodeld` добавлен в таргет `FinanceFlowWidget`:
1. Выберите файл `FinanceFlowModel.xcdatamodeld`
2. В File Inspector → Target Membership
3. Отметьте `FinanceFlowWidget`

### 5. Настроить CloudKit (опционально)

1. Выберите таргет `FinanceFlow`
2. Signing & Capabilities → + Capability → CloudKit
3. Создайте Container: `iCloud.com.financeflow.app`

## 🧪 Тестирование

### Виджет:
1. Запустите приложение
2. Добавьте несколько транзакций
3. Долго нажмите на главный экран
4. Нажмите "+" → Найдите "FinanceFlow Widget"
5. Добавьте виджет
6. Проверьте отображение баланса

### Siri Shortcuts:
1. Settings → Siri & Search
2. Найдите "FinanceFlow"
3. Добавьте shortcuts
4. Протестируйте:
   - "Hey Siri, add expense 50 in FinanceFlow"
   - "Hey Siri, show balance in FinanceFlow"

## 📝 Примечания

- Виджет обновляется каждые 15 минут автоматически
- Баланс кэшируется для быстрого доступа из Siri
- Транзакции из Siri обрабатываются при следующем открытии приложения
- Все данные синхронизируются через App Group

## 🐛 Решение проблем

### Виджет не показывает данные:
- Проверьте, что App Group настроен для всех таргетов
- Убедитесь, что `SharedData.swift` использует правильный App Group ID
- Проверьте, что Core Data модель добавлена в виджет

### Siri Shortcuts не работают:
- Убедитесь, что App Intents Extension добавлен в проект
- Проверьте Bundle Identifier
- Перезапустите устройство

### Ошибки компиляции:
- Убедитесь, что все файлы добавлены в правильные таргеты
- Проверьте, что зависимости подключены
- Очистите Build Folder (Cmd+Shift+K)


